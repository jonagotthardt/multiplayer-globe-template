{
  "name": "multiplayer-globe-template",
  "description": "CommunitySMP multi-feature companion website for players with tools, guides, and live data.",
  "dependencies": {
    "partyserver": "0.0.66",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "esbuild": "0.25.2",
    "typescript": "5.8.3",
    "wrangler": "4.21.x"
  },
  "scripts": {
    "cf-typegen": "wrangler types ./src/server/worker-configuration.d.ts",
    "check": "tsc --project src/client && tsc --project src/server && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
